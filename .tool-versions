# tuikit tool versions managed by asdf
# See https://asdf-vm.com for usage
# Install all: asdf install
#
# Go-based tools (golangci-lint, gofumpt, fieldalignment) are declared in
# go.mod's `tool` block — invoke them via `go tool <name>`.

golang 1.26.6
